# M4 V3 Work Log — 2026-08-20

## Branch

`v3-bull-pullback-recovery`

Base state before the V3 cycle: `0c108182eb778688570950ee92e0c13d1a16d4e7`
(`Record rejected Trend V2 development cycle`).

## V3 status

The materially new `Bull Pullback Recovery` research cycle has started.

Completed on this branch:

- frozen hypothesis contract: `docs/M4_V3_BULL_PULLBACK_RECOVERY_HYPOTHESIS.md`;
- policy SHA-256: `e10448c974bc6ff74cabe9f2ca0616f67c78c212457bd2fa198af7776b805feb`;
- freeze manifest: `docs/M4_V3_BULL_PULLBACK_RECOVERY_POLICY_FREEZE.json`;
- pure frozen policy/constants module: `src/eba_trader/v3_pullback_policy.py`;
- deterministic pullback/recovery engine scaffold: `src/eba_trader/v3_pullback.py`;
- policy and core behavior tests: `tests/test_v3_pullback_policy.py` and `tests/test_v3_pullback.py`.

## Methodology boundary

V3 is not a retune of Trend V1/V2. It uses:

`4h bull regime -> bounded 15m pullback below rolling VWAP -> causal recovery confirmation -> next-open entry`

The baseline uses a structural swing-low stop, fixed 2R target, time exit, regime exit, Spot-only
risk caps and unchanged cost stress assumptions.

## Critical locks

- 2025 OOS: `LOCKED_NOT_ACCESSED`.
- AI/news/funding/basis/liquidation inputs: excluded from the baseline V3 cycle.
- Parameter changes after first V3 development run: forbidden.
- No V3 historical evidence has been generated yet.
- No PAPER/SHADOW/MICRO_LIVE/real-money authority exists.

## Next strict step

Before adding evidence/verdict code or running historical development data:

1. run the full repository pytest suite on supported Python 3.12;
2. run Ruff;
3. fix only implementation defects that violate the already-frozen contract;
4. keep the policy document and parameters unchanged;
5. only after deterministic tests are green, add V3 evidence/robustness/verdict wiring;
6. only after that run cached 2021–2024 development data;
7. never access 2025 OOS during this cycle unless a later fully eligible final-freeze review explicitly authorizes it.
