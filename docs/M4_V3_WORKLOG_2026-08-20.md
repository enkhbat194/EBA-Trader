# M4 V3 Work Log — 2026-08-20

## Branch

`v3-bull-pullback-recovery`

Base state before the V3 cycle: `0c108182eb778688570950ee92e0c13d1a16d4e7`
(`Record rejected Trend V2 development cycle`).

## V3 status

The materially new `Bull Pullback Recovery` research cycle is frozen and implemented on this feature branch, but historical development evidence has not been opened yet.

Completed on this branch:

- frozen hypothesis contract: `docs/M4_V3_BULL_PULLBACK_RECOVERY_HYPOTHESIS.md`;
- policy SHA-256: `e10448c974bc6ff74cabe9f2ca0616f67c78c212457bd2fa198af7776b805feb`;
- freeze manifest: `docs/M4_V3_BULL_PULLBACK_RECOVERY_POLICY_FREEZE.json`;
- frozen policy/constants module: `src/eba_trader/v3_pullback_policy.py`;
- deterministic pullback/recovery engine: `src/eba_trader/v3_pullback.py`;
- policy, causal entry/exit, source-gap, risk-sizing and screening tests;
- fail-closed development evidence and 34-gate screening: `src/eba_trader/v3_pullback_evidence.py`;
- public development command: `eba-v3-development`;
- native Windows runner: `scripts/run_v3_development.ps1`;
- Bash/Linux runner: `scripts/run_v3_development.sh`;
- GitHub Actions Python 3.12 pytest/Ruff validation workflow.

## Methodology boundary

V3 is not a retune of Trend V1/V2. It uses:

`4h bull regime -> bounded 15m pullback below rolling VWAP -> causal recovery confirmation -> next-open entry`

The baseline uses a structural swing-low stop, fixed 2R target, time exit, regime exit, Spot-only risk caps and unchanged cost stress assumptions.

The engine additionally fails closed around source gaps: an armed setup is cancelled when source continuity breaks, and a pending next-open entry cannot execute unless current source health and the frozen bull regime are still valid.

## Critical locks

- 2025 OOS: `LOCKED_NOT_ACCESSED`.
- AI/news/funding/basis/liquidation inputs: excluded from the baseline V3 cycle.
- Parameter changes after the first V3 development run: forbidden.
- No V3 historical evidence or V3 backtest result has been generated yet.
- No PAPER/SHADOW/MICRO_LIVE/real-money authority exists.

## Validation status

The code and tests have been assembled on GitHub. A GitHub Actions workflow exists, but no successful CI result has been observed for the latest branch head yet. Therefore this log does **not** claim that the latest complete repository pytest/Ruff suite has passed.

The next runtime validation must use supported Python 3.12 and must not modify the frozen policy to make tests or results pass.

## Next strict step

1. Run the complete pytest suite on the latest branch head using Python 3.12.
2. Run Ruff on the same commit.
3. Fix only implementation defects that violate the already-frozen contract; do not change V3 parameters or gates.
4. When tests and Ruff are green on a clean tracked tree, run `scripts/run_v3_development.ps1` on Windows or `scripts/run_v3_development.sh` on Bash/Linux.
5. The development runner may read only the already-frozen 2021–2024 research/validation datasets and must verify their hashes.
6. If any signal gate 1–21 fails, stop the cycle and leave risk gates 22–34 blocked.
7. Run the risk-sized layer only if all signal gates pass.
8. Keep 2025 OOS locked regardless of a development pass until a later explicit final-freeze review.
