name: M8 frozen historical data audit (disabled after incomplete attempt)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'M8 audit temporarily disabled while an implementation boundary defect is corrected.'
