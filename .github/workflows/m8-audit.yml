name: M8 frozen historical data audit — CLOSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  closed:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'M8 historical audit is closed.'
          echo 'Authoritative run: 32433740347'
          echo 'Evidence SHA-256: 1bcfd0f44917d608b0d0c413d22aa7ce851e55ee4d54b1b81f87f588682a887f'
          echo 'Do not rerun or weaken the frozen M8 contract.'
